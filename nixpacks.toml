[phases.setup]
nixPkgs = ["ffmpeg", "imagemagick", "python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install -r backend/requirements.txt"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
